export const metadata = {
  title: 'Whitepaper - Purify Protocol',
  description: 'Official whitepaper for Purify Protocol - The Green Blockchain Recycling Protocol',
};

export default function WhitepaperLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}